{
  "name": "spll",
  "version": "1.0.0",
  "private": true,
  "description": "SPLL システム（Google Apps Script + clasp）／ SPLL-SYS-BD-001",
  "scripts": {
    "login": "node scripts/clasp.js login",
    "logout": "node scripts/clasp.js logout",
    "build": "node scripts/build.js",
    "test": "node scripts/test-all.js",
    "push:portal": "npm run build && cd apps/portal && clasp push -f",
    "push:workflow": "npm run build && cd apps/workflow && clasp push -f",
    "push:admin": "npm run build && cd apps/admin && clasp push -f",
    "push:all": "npm run build && cd apps/portal && clasp push -f && cd ../workflow && clasp push -f && cd ../admin && clasp push -f",
    "deploy:portal": "npm run build && node scripts/deploy.js portal",
    "deploy:workflow": "npm run build && node scripts/deploy.js workflow",
    "deploy:admin": "npm run build && node scripts/deploy.js admin",
    "deploy:all": "npm run build && node scripts/deploy.js portal && node scripts/deploy.js workflow && node scripts/deploy.js admin",
    "web:dev": "node apps/public-web/server.js",
    "web:build": "docker build -f apps/public-web/Dockerfile -t spll-public-web ."
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "marked": "^18.0.6",
    "mermaid": "^11.16.0"
  },
  "engines": {
    "node": ">=14"
  }
}
