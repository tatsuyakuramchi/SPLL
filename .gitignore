# clasp: 実プロジェクトの scriptId を含むため、リポジトリにはテンプレートのみを置く
.clasp.json
.clasprc.json

# 秘密情報は GAS の ScriptProperties に置く。万一ローカルに置いた場合の保険
.env
*.local
secrets.json

# OS / editor
.DS_Store
Thumbs.db
node_modules/
*.log

# docsのプレビュー画像（ビルド生成物）
docs/_preview*.png
